(function(){"use strict";function esc(s){return String(s==null?"":s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;")}function renderBio(data){const section=document.getElementById("bio-section");if(!section)return;section.innerHTML="<h2>Bio</h2>"+"<p>"+esc(data.bio||"")+"</p>"}function renderSocials(data){const section=document.getElementById("socials-section");if(!section||!Array.isArray(data.socials))return;if(data.socials.length===0){section.innerHTML='<h2>Socials</h2><span class="social-links"></span>';return}const links=data.socials.map(function(s){const label=esc(s.label||"");const url=esc(s.url||"#");const open=/^https?:\/\//.test(s.url||"");const icon=s.icon?'<span class="social-icon">'+esc(s.icon)+"</span>":"";return'<a href="'+url+'"'+(open?' target="_blank" rel="noopener"':"")+">"+icon+label+"</a>"});const separator='<span class="social-separator" aria-hidden="true">-</span>';const html=[];for(let i=0;i<links.length;i++){if(i>0)html.push(separator);html.push(links[i])}section.innerHTML="<h2>Socials</h2>"+'<span class="social-links">'+html.join("")+"</span>"}function renderSkills(data){const section=document.getElementById("skills-section");if(!section||!Array.isArray(data.skills_groups))return;const groupsHTML=data.skills_groups.map(function(g){const items=Array.isArray(g.items)?g.items:[];const chipsHTML=items.map(function(it){return'<span class="skill-chip">'+esc(it)+"</span>"}).join("");return'<div class="skill-group">'+"<h3>"+esc(g.category||"")+"</h3>"+'<div class="skill-chips">'+chipsHTML+"</div>"+"</div>"}).join("");section.innerHTML="<h2>Skills &amp; Tools</h2>"+groupsHTML}async function loadBio(){try{const res=await fetch("data/bio.json");if(!res.ok)throw new Error("HTTP "+res.status);const data=await res.json();renderBio(data);renderSocials(data);renderSkills(data)}catch(err){console.warn("Bio JSON load failed; using fallback:",err.message)}}if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",loadBio)}else{loadBio()}})();