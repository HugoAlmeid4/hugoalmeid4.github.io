// ─── Giscus configuration ──────────────────────────────────────────────────
// Comments live in GitHub Discussions, not a separate database. To enable:
//   1. Enable Discussions on https://github.com/HugoAlmeid4/hugoalmeid4.github.io
//      (Settings → Features → check "Discussions", create a default category if
//      prompted).
//   2. Visit https://giscus.app/, fill in the repo + category + mapping.
//   3. Copy the `data-repo-id` and `data-category-id` values from the script
//      snippet giscus.app generates.
//   4. Paste them below. Comments will start working on next page load.
//
// Leave REPO_ID and CATEGORY_ID blank to show a setup hint instead of
// attempting to load giscus. ─────────────────────────────────────────────────
window.GISCUS_REPO = 'HugoAlmeid4/hugoalmeid4.github.io';
window.GISCUS_REPO_ID = '';       // e.g. 'R_kgDOL...'
window.GISCUS_CATEGORY = 'General';
window.GISCUS_CATEGORY_ID = '';   // e.g. 'DIC_kwDOL...'
window.GISCUS_LANG = 'en';
window.GISCUS_THEME = 'preferred_color_scheme'; // 'light' | 'dark' | 'preferred_color_scheme'

// Reserved for future use — do not delete, even if empty.
window.GISCUS_REACTIONS_ENABLED = '1';
window.GISCUS_EMIT_METADATA = '0';
window.GISCUS_INPUT_POSITION = 'top';
